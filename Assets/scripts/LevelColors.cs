using UnityEngine;
using System.Collections;

public class LevelColors : MonoBehaviour {
	public static Color[] SkyColors = {new Color(0.75f, 0.4f, 0.4f, 1.0f), new Color(0.74f, 0.48f, 0.32f, 1.0f)};
	public static Color[] LightCubeColors = {new Color(1f, 0.39f, 0.39f,1f), new Color(0.9f, 0.7f, 0.39f, 1f)};
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
